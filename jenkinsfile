pipeline {
    agent any

    stages {
        stage('checkout code'){
            steps{
                 git 'https://github.com/Houssem123987/houssem'

        stage('Build') {
            steps {
                sh 'echo "Building the app"'
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Testing the app"'
            }
        }

        stage('Deploy') {
            steps {
                sh 'echo "Deploying the app"'
            }
        }
    }
}
